{
  "name": "ouyu-rental-manager",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ouyu-db --file=db/schema.sql",
    "db:init:prod": "wrangler d1 execute ouyu-db --file=db/schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
